
import"./chunk-A7SHUGSC.mjs";import{open,stat}from"node:fs/promises";import path from"node:path";var ACCESS_GUIDE_FILENAME="ADAPT_Access_Patterns_v2.pdf";var ACCESS_GUIDE_DOWNLOAD_PATH="/api/admin/access-guide";var ACCESS_GUIDE_META_PATH="/api/admin/access-guide/meta";function accessGuideAssetPath(env=process.env,cwd=process.cwd()){const directory=env.NODE_ENV?.trim().toLowerCase()==="production"?"assets":"docs";return path.join(cwd,directory,ACCESS_GUIDE_FILENAME)}async function accessGuideAvailable(assetPath){try{return(await stat(assetPath)).isFile()}catch{return false}}function unavailable(res){res.status(404).json({error:"access_guide_unavailable",detail:"The access guide is not available in this build."})}function setupAccessGuideRoutes(appkit,options={}){const assetPath=options.assetPath??accessGuideAssetPath();appkit.server.extend(app=>{app.get(ACCESS_GUIDE_META_PATH,async(_req,res)=>{res.setHeader("Cache-Control","private, no-store");res.json({available:await accessGuideAvailable(assetPath)})});app.get(ACCESS_GUIDE_DOWNLOAD_PATH,async(_req,res)=>{let file;try{file=await open(assetPath,"r");const info=await file.stat();if(!info.isFile()){await file.close();unavailable(res);return}res.setHeader("Content-Type","application/pdf");res.setHeader("Content-Disposition",`attachment; filename="${ACCESS_GUIDE_FILENAME}"`);res.setHeader("Content-Length",String(info.size));res.setHeader("Cache-Control","private, no-store");res.setHeader("X-Content-Type-Options","nosniff");res.setHeader("Cross-Origin-Resource-Policy","same-origin");const stream=file.createReadStream({autoClose:false});let closing=null;const closeFile=()=>{closing??=file?.close().then(()=>void 0).catch(()=>void 0)??Promise.resolve();return closing};stream.on("error",error=>{void closeFile();if(!res.headersSent)unavailable(res);else res.destroy(error)});stream.on("end",()=>void closeFile());res.on("close",()=>{if(!stream.destroyed)stream.destroy();void closeFile()});stream.pipe(res)}catch{if(file)await file.close().catch(()=>void 0);unavailable(res)}})})}export{ACCESS_GUIDE_DOWNLOAD_PATH,ACCESS_GUIDE_FILENAME,ACCESS_GUIDE_META_PATH,accessGuideAssetPath,setupAccessGuideRoutes};
