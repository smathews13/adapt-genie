
import{ExpiringLruCache}from"./chunk-66G4LGYE.mjs";import{CHANGED_BY,CONNECTED_RESOURCES,DEFAULT_JUDGE_ENDPOINT,connectedResource,withDeadline}from"./chunk-LOICDKUA.mjs";import{APP_SCHEMA}from"./chunk-YDSOP3SS.mjs";var EXPERIMENT_PATH="/api/2.0/mlflow/experiments/get";var EXPERIMENT_BY_NAME_PATH="/api/2.0/mlflow/experiments/get-by-name";var EXPERIMENT_PROBE_TIMEOUT_MS=5e3;function textOf(value){return typeof value==="string"?value.trim():""}function experimentOf(body){return body.experiment??body}function experimentIdOf(body){return textOf(experimentOf(body).experiment_id)}function describes(body){const experiment=experimentOf(body);const name=textOf(experiment.name);const stage=textOf(experiment.lifecycle_stage);if(name&&stage)return`${name}, ${stage}`;return name||stage}var ACTIVE_STAGE="active";function experimentVerdict(input){const experimentId=input.experimentId.trim();const base={id:"experiment-id",kind:"observability",name:experimentId,label:"MLflow experiment",checked_with:`Read as the application, not as you: GET ${EXPERIMENT_PATH}`,duration_ms:input.durationMs??0,error:"",remedy:null};if(!experimentId){return{...base,status:"failed",detail:"No MLflow experiment is configured. The application cannot resolve a trace destination until an experiment id or path is configured.",error:"no MLflow experiment is configured"}}if(input.read.kind==="no-response"){return{...base,status:"unverified",detail:"The workspace could not be asked about the experiment, so nothing was established either way.",error:input.read.message}}if(input.read.kind==="refused"){const{status,code,message}=input.read;const refusal=`HTTP ${status}${code?` ${code}`:""}`;return{...base,status:"failed",detail:`${refusal}: ${message||"the workspace gave no message"}. Read as the application, not as you.`,error:message||refusal}}const observed=describes(input.read.body);const displayName=textOf(experimentOf(input.read.body).name);const stage=textOf(experimentOf(input.read.body).lifecycle_stage);if(stage&&stage.toLowerCase()!==ACTIVE_STAGE){return{...base,display_name:displayName||void 0,status:"failed",detail:`Read as the application, not as you${observed?`: ${observed}`:""}. The experiment exists but is ${stage}, not ${ACTIVE_STAGE}, so runs cannot be logged to it and the trace of every run is being dropped. Restore it in the workspace, or point PLAYER_INSIGHTS_EXPERIMENT_ID at one that is live.`,error:`the experiment is ${stage}`}}return{...base,display_name:displayName||void 0,status:"ok",detail:`Read as the application, not as you${observed?`: ${observed}`:""}. Traces have somewhere to land; whether you can open it is your own grant.`}}var workspaceExperimentReader=async experimentId=>{try{const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");const client=new WorkspaceClient({});const body=await client.apiClient.request({path:EXPERIMENT_PATH,method:"GET",query:{experiment_id:experimentId},headers:new Headers({Accept:"application/json"}),raw:false});return{kind:"ok",body:body??{}}}catch(error){return readFailure(error)}};var workspaceExperimentIdResolver=async experimentPath=>{const path=experimentPath.trim();if(!path)return"";try{const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");const client=new WorkspaceClient({});const body=await client.apiClient.request({path:EXPERIMENT_BY_NAME_PATH,method:"GET",query:{experiment_name:path},headers:new Headers({Accept:"application/json"}),raw:false});return experimentIdOf(body??{})}catch{return""}};function readFailure(error){const shape=error??{};const status=Number(shape.statusCode??shape.status??0);const message=textOf(shape.message)||"the call did not complete";if(Number.isFinite(status)&&status>=400){return{kind:"refused",status,code:textOf(shape.errorCode),message}}return{kind:"no-response",message}}async function checkExperimentAsApp(experimentId,read=workspaceExperimentReader,timeoutMs=EXPERIMENT_PROBE_TIMEOUT_MS){const id=experimentId.trim();const started=Date.now();if(!id){return experimentVerdict({experimentId:id,read:{kind:"no-response",message:"no MLflow experiment is configured"},durationMs:0})}let outcome;try{outcome=await withDeadline(read(id),timeoutMs,`The MLflow experiment lookup did not answer within ${timeoutMs} ms.`)}catch(error){outcome=readFailure(error)}return experimentVerdict({experimentId:id,read:outcome,durationMs:Date.now()-started})}var DIRTY_SUFFIX="+dirty";var MIN_ABBREV=7;function commitOf(sha){const trimmed=sha.trim();return trimmed.endsWith(DIRTY_SUFFIX)?trimmed.slice(0,-DIRTY_SUFFIX.length):trimmed}function parseAncestorList(raw){if(!raw?.trim())return[];return raw.trim().split(/[\s,]+/).map(entry=>commitOf(entry)).filter(entry=>entry.length>=MIN_ABBREV)}var DEPLOYMENT_SETTINGS_DDL=`CREATE TABLE IF NOT EXISTS ${APP_SCHEMA}.deployment_settings (resource_id TEXT PRIMARY KEY,
     value TEXT NOT NULL,
     intent TEXT NOT NULL,
     note TEXT NOT NULL DEFAULT '',
     updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
     updated_by TEXT NOT NULL
   )`;var STORED_SETTINGS_QUERY=`
  SELECT resource_id, value, intent, note, updated_at, updated_by
  FROM ${APP_SCHEMA}.deployment_settings
  ORDER BY resource_id`;var UPSERT_SETTING_QUERY=`
  INSERT INTO ${APP_SCHEMA}.deployment_settings (resource_id, value, intent, note, updated_by, updated_at)
  VALUES ($1, $2, $3, $4, $5, now())
  ON CONFLICT (resource_id) DO UPDATE
    SET value = EXCLUDED.value,
        intent = EXCLUDED.intent,
        note = EXCLUDED.note,
        updated_by = EXCLUDED.updated_by,
        updated_at = now()
  RETURNING resource_id, value, intent, note, updated_at, updated_by`;var DELETE_SETTING_QUERY=`
  DELETE FROM ${APP_SCHEMA}.deployment_settings WHERE resource_id = $1 RETURNING resource_id`;function text(value){if(typeof value==="string")return value;if(typeof value==="number"||typeof value==="boolean")return String(value);return null}function storedFromRow(row){const updatedAt=row.updated_at;return{resourceId:text(row.resource_id)??"",value:text(row.value)??"",intent:row.intent==="active"?"active":"intended",note:text(row.note)??"",updatedAt:updatedAt instanceof Date?updatedAt.toISOString():text(updatedAt)??"",updatedBy:text(row.updated_by)??""}}var STORED_SETTINGS_TTL_MS=45e3;var settingsCache=new WeakMap;function forgetStoredSettings(){settingsCache=new WeakMap}async function readStoredSettings(client,options={}){const maxAge=options.maxAgeMs??0;const now=options.now??Date.now();const cached=settingsCache.get(client);if(cached&&maxAge>0&&now-cached.at<maxAge)return cached.settings;try{const result=await client.lakebase.query(STORED_SETTINGS_QUERY);const rows=result?.rows??[];const settings=new Map(rows.map(row=>{const setting=storedFromRow(row);return[setting.resourceId,setting]}));settingsCache.set(client,{at:now,settings});return settings}catch(error){console.warn("[settings] Stored settings could not be read:",error.message);return new Map}}async function writeStoredSetting(client,setting){const result=await client.lakebase.query(UPSERT_SETTING_QUERY,[setting.resourceId,setting.value,setting.intent,setting.note,setting.updatedBy]);const row=(result?.rows??[])[0];if(!row)throw new Error("the settings row was not written back");forgetStoredSettings();return storedFromRow(row)}async function clearStoredSetting(client,resourceId){const result=await client.lakebase.query(DELETE_SETTING_QUERY,[resourceId]);forgetStoredSettings();return(result?.rows??[]).length>0}async function resolveJudgeEndpoint(client){const stored=await readStoredSettings(client,{maxAgeMs:STORED_SETTINGS_TTL_MS});const saved=stored.get("judge-endpoint");if(saved?.intent==="active"&&saved.value)return saved.value;return process.env.PLAYER_INSIGHTS_JUDGE_ENDPOINT?.trim()||DEFAULT_JUDGE_ENDPOINT}var EXPERIMENT_ID_CACHE_MAX_ENTRIES=128;var EXPERIMENT_ID_CACHE_TTL_MS=60*6e4;var experimentIdByPath=new ExpiringLruCache(EXPERIMENT_ID_CACHE_MAX_ENTRIES,EXPERIMENT_ID_CACHE_TTL_MS);function forgetResolvedExperimentIds(){experimentIdByPath.clear()}async function resolveExperimentId(client,resolvePath=workspaceExperimentIdResolver,now=Date.now()){const stored=await readStoredSettings(client,{maxAgeMs:STORED_SETTINGS_TTL_MS});const saved=stored.get("experiment-id");if(saved?.intent==="active"&&saved.value)return saved.value;const fromEnv=process.env.PLAYER_INSIGHTS_EXPERIMENT_ID?.trim();if(fromEnv)return fromEnv;const path=process.env.PLAYER_INSIGHTS_EXPERIMENT_PATH?.trim();if(!path)return"";const cached=experimentIdByPath.get(path,now);if(cached)return cached;const resolved=(await resolvePath(path)).trim();if(resolved)experimentIdByPath.set(path,resolved,now);return resolved}async function resolveNotebookDeclaration(client){const stored=await readStoredSettings(client,{maxAgeMs:STORED_SETTINGS_TTL_MS});const saved=stored.get("notebook-declaration");if(saved?.intent==="active"&&saved.value)return saved.value;return process.env.PLAYER_INSIGHTS_NOTEBOOK_DECLARATION?.trim()??""}function appBuildSha(){return process.env.PLAYER_INSIGHTS_BUILD_SHA?.trim()??""}function appBuildAncestors(){return parseAncestorList(process.env.PLAYER_INSIGHTS_BUILD_ANCESTORS)}function appEnvironment(){const values={};for(const resource of CONNECTED_RESOURCES){if(!resource.appEnvVar)continue;values[resource.appEnvVar]=process.env[resource.appEnvVar]?.trim()??""}return values}var APP_DEFAULTS={"llm-endpoint":"databricks-claude-sonnet-4-6","judge-endpoint":DEFAULT_JUDGE_ENDPOINT,"shared-conversation-rail":"false","lakebase-schema":APP_SCHEMA};function namespaceInUse(checks){const prefixes=new Set;for(const check of checks){if(check.kind!=="table"||!check.name)continue;const parts=check.name.split(".");if(parts.length===3)prefixes.add(`${parts[0]}.${parts[1]}`)}if(prefixes.size!==1)return null;const[catalog,schema]=[...prefixes][0].split(".");return{catalog,schema}}function displayValue(value){if(Array.isArray(value)){const entries=[];for(const item of value){const entry=text(item);if(entry===null)return"";entries.push(entry)}return entries.join(", ")}return text(value)??""}function resourceStates(input){const{report,environment,stored}=input;const byCheck=new Map((report?.checks??[]).map(check=>[check.id,check]));const configuration=new Map((report?.configuration??[]).map(entry=>[String(entry.key),entry]));const namespace=namespaceInUse(report?.checks??[]);return CONNECTED_RESOURCES.map(resource=>{const entry=resource.agentKey?configuration.get(resource.agentKey):void 0;const check=resource.actualFromCheck?byCheck.get(resource.actualFromCheck):void 0;const saved=stored.get(resource.id);let configured=entry?displayValue(entry.value):"";let configuredFrom=entry?text(entry.source)??"":"";if(!configured&&resource.appEnvVar){configured=environment[resource.appEnvVar]??"";configuredFrom="app-environment"}if(resource.id==="lakebase-schema"){configured=APP_SCHEMA;if(!configuredFrom)configuredFrom="app-environment"}if(!configured&&resource.id in APP_DEFAULTS){configured=APP_DEFAULTS[resource.id];configuredFrom="app-default"}if(saved?.intent==="active"&&saved.value){configured=saved.value;configuredFrom="app-saved"}let actual=check?.name??"";let actualObserved=Boolean(check&&check.name);if(!actualObserved&&namespace){if(resource.id==="catalog"){actual=namespace.catalog;actualObserved=true}else if(resource.id==="schema"){actual=namespace.schema;actualObserved=true}}return{resource,configured,configuredFrom,actual,actualObserved,intended:saved&&saved.intent==="intended"?saved.value:null,intendedAt:saved?.updatedAt??"",intendedBy:saved?.updatedBy??"",editable:CHANGED_BY[resource.changedBy].appliesImmediately}})}var ARTIFACT="artifact";var TRUSTED_PROVENANCE=new Set([ARTIFACT,"app-environment","app-default","data-contract"]);function computeDrift(input){const{report,states,endpointAnswered}=input;const findings=[];if(!report){if(endpointAnswered!==true){findings.push({id:"orchestrator-unreachable",severity:"unknown",resourceId:"agent-endpoint",headline:"The agent did not answer, so nothing below could be checked",detail:"The serving endpoint did not reply, so the values below are only what this deployment was set up with. None of them have been checked against anything, and an answer asked right now would probably fail too.",remedy:"Fix the blocked checks under “What to fix” above, then re-check."})}return findings}if(report.source==="agent"&&(!report.configuration||report.configuration.length===0)){findings.push({id:"configuration-unreported",severity:"unknown",resourceId:null,headline:"The served model version does not report its own configuration",detail:"This endpoint answered, but the model version running on it was logged before the configuration report existed. What the orchestrator was configured with cannot be read from it, only what the checks below proved it could reach. The two are not the same claim.",remedy:"Log and roll out a model version from a build that carries the report."})}for(const state of states){if(!state.resource.agentKey||!state.configuredFrom)continue;if(TRUSTED_PROVENANCE.has(state.configuredFrom))continue;if(!state.configured)continue;findings.push({id:`provenance-${state.resource.id}`,severity:"blocking",resourceId:state.resource.id,headline:`${state.resource.label} did not come from the model artifact`,detail:`The orchestrator resolved this from ${state.configuredFrom}, not from the model version it is serving. Nothing in the registry records where that value came from, and the resources automatic authentication passthrough granted this version were named from the artifact, so what it is pointed at and what it is permitted to reach can differ.`,remedy:state.resource.applyWith})}for(const state of states){if(!state.actualObserved||!state.configured)continue;if(state.actual===state.configured)continue;findings.push({id:`mismatch-${state.resource.id}`,severity:"blocking",resourceId:state.resource.id,headline:`${state.resource.label} in use is not the one configured`,detail:`Configured as ${state.configured}, but the check that ran inside the endpoint used ${state.actual}. The running system is not doing what this deployment's configuration says.`,remedy:state.resource.applyWith})}for(const state of states){if(!state.intended)continue;const inForce=state.actualObserved?state.actual:state.configured;if(state.intended===inForce)continue;findings.push({id:`pending-${state.resource.id}`,severity:"pending",resourceId:state.resource.id,headline:`${state.resource.label} has an intended value that is not in effect`,detail:`Saved as ${state.intended}${state.intendedBy?` by ${state.intendedBy}`:""}, while the deployment is using ${inForce||"(nothing)"}. Saving it here recorded the intention; it changed nothing about the running system.`,remedy:state.resource.applyWith})}return findings}function driftStatus(findings){if(findings.some(finding=>finding.severity==="blocking"))return"blocked";if(findings.some(finding=>finding.severity==="unknown"))return"unknown";if(findings.some(finding=>finding.severity==="pending"))return"pending";return"ok"}function settingsPayload(input){const states=resourceStates(input);const drift=computeDrift({report:input.report,states,endpointAnswered:input.endpointAnswered});return{resources:states.map(state=>({...state,changedByLabel:CHANGED_BY[state.resource.changedBy].label,changedByNote:CHANGED_BY[state.resource.changedBy].note})),drift,status:driftStatus(drift),appBuildSha:input.appBuildSha,appBuildAncestors:[...input.appBuildAncestors??[]],modelBuildSha:input.report?.build_sha??"",orchestratorReported:Boolean(input.report?.configuration?.length),storeAvailable:input.storeAvailable,checkedAt:new Date().toISOString(),app:input.app}}function classifyWrite(resourceId,requested){const resource=connectedResource(resourceId);if(!resource)return{ok:false,reason:`${resourceId} is not a resource this deployment has.`};const tier=CHANGED_BY[resource.changedBy];if(requested==="active"&&!tier.appliesImmediately){return{ok:false,reason:`${resource.label} cannot be changed by saving a value: ${tier.note} Save it as an intended value instead, then apply it with: ${resource.applyWith}`}}if(requested==="intended"){return{ok:true,intent:"intended",changedBy:resource.changedBy}}return{ok:true,intent:requested,changedBy:resource.changedBy}}var ACCESS_GATE_ENABLED=false;var ACCESS_MODES=["service-principal","user-verified","skipped"];function isAccessMode(value){return typeof value==="string"&&ACCESS_MODES.includes(value)}function appServicePrincipal(){return process.env.DATABRICKS_CLIENT_ID?.trim()||null}var servingPrincipal=null;function observedServingPrincipal(){return servingPrincipal}var ACCESS_DECISION_TTL_MS=5*6e4;var ACCESS_DECISION_CACHE_MAX_ENTRIES=2048;var decisions=new ExpiringLruCache(ACCESS_DECISION_CACHE_MAX_ENTRIES,ACCESS_DECISION_TTL_MS);function decisionKey(email){return email.trim().toLowerCase()}function declareAccessMode(email,mode,detail,now=Date.now()){if(mode==="user-verified"){throw new Error("user-verified is established by running the access checks, not by declaring it. Call recordVerifiedAccess with the outcome of a real check.")}const decision={mode,decidedAt:new Date(now).toISOString(),detail};decisions.set(decisionKey(email),decision,now);return decision}function recordVerifiedAccess(email,detail,now=Date.now()){const decision={mode:"user-verified",decidedAt:new Date(now).toISOString(),detail};decisions.set(decisionKey(email),decision,now);return decision}function accessModeFor(email,now=Date.now()){return decisions.get(decisionKey(email),now)?.mode??"service-principal"}function accessDecisionFor(email,now=Date.now()){return decisions.get(decisionKey(email),now)??null}function recordedAccessMode(email,gate=ACCESS_GATE_ENABLED,now=Date.now()){if(gate)return accessModeFor(email,now);return accessDecisionFor(email,now)?.mode??null}function executionIdentityColumns(email,execution){const serving=observedServingPrincipal();return[appServicePrincipal(),serving?.id??null,serving?.observedAt??null,recordedAccessMode(email),execution?.mode??null,execution?.verified??null]}export{checkExperimentAsApp,DEPLOYMENT_SETTINGS_DDL,forgetStoredSettings,readStoredSettings,writeStoredSetting,clearStoredSetting,resolveJudgeEndpoint,forgetResolvedExperimentIds,resolveExperimentId,resolveNotebookDeclaration,appBuildSha,appBuildAncestors,appEnvironment,resourceStates,settingsPayload,classifyWrite,isAccessMode,appServicePrincipal,observedServingPrincipal,declareAccessMode,recordVerifiedAccess,accessModeFor,accessDecisionFor,executionIdentityColumns};
