
async function withDeadline(work,ms,message){let timer;try{return await Promise.race([work,new Promise((_resolve,reject)=>{timer=setTimeout(()=>reject(new Error(message)),ms);timer.unref?.()})])}finally{if(timer)clearTimeout(timer)}}var MLFLOW_JUDGE_PROMPT_VERSION="mlflow-3.14.0";var DEFAULT_JUDGE_ENDPOINT="databricks-claude-sonnet-4-5";function judgeBadgeLabel(judgeEndpoint){return`LLM judge · MLflow prompt · ${judgeEndpoint}`}function judgeDisclosure(judgeEndpoint){return`Scored by MLflow ${MLFLOW_JUDGE_PROMPT_VERSION.replace("mlflow-","")}'s published judge prompts, run against the ${judgeEndpoint} serving endpoint through the same transport the app uses to call the agent. This is not the Databricks managed judge service: the prompts and the yes/no parsing are MLflow's, the model answering them is ${judgeEndpoint}.`}var GROUNDEDNESS_BASIS="The groundedness document is what the agent's own trace disclosed for that answer: its stage output, the SQL it ran, and the figures and sources it returned. A `no` therefore means the answer asserted something the app cannot substantiate from what it showed the user, which is not the same as contradicting the underlying data.";function judgeProvenance(judgeName,judgeEndpoint){return`MLflow ${MLFLOW_JUDGE_PROMPT_VERSION.replace("mlflow-","")} ${judgeName} prompt, run against databricks:/${judgeEndpoint}`}var BUDGET_TRUNCATION_CODE="SUITE_BUDGET_EXHAUSTED";var SUITE_CANCELLED_CODE="SUITE_CANCELLED";var BENCHMARK_RUNNER_VERSION="benchmark-runner-2";var CHANGED_BY={"model-version":{label:"New model version",note:"Baked into the MLflow model artifact when the agent was logged. No form can change it: the same values name the resources automatic authentication passthrough grants this version, so a runtime override could aim the orchestrator at a warehouse it has no permission to use.",appliesImmediately:false},"app-redeploy":{label:"App redeploy",note:"Arrives as an environment variable in the app container, written into the deployed app.yaml at release time. Set it in the bundle target and release the app.",appliesImmediately:false},"app-runtime":{label:"Editable here",note:"The app reads this on every request, so a value saved here takes effect immediately.",appliesImmediately:true},"app-source":{label:"Edit app source",note:"A literal in application source with no variable that overrides it. Changing it means editing the source and redeploying.",appliesImmediately:false},"agent-environment":{label:"Not reachable in serving",note:"Read from the orchestrator process environment, which a served entity does not inherit from anything a deployer controls. Inside the endpoint it is always the compiled default.",appliesImmediately:false}};var AGENT_RELEASE="TARGET=<target> bundle/agent-release.sh --apply";var APP_RELEASE="TARGET=<target> bundle/app-release.sh --apply";var CONNECTED_RESOURCES=[{id:"agent-endpoint",label:"adapt-orchestrator",kind:"agent",changedBy:"app-redeploy",arrivesBy:"The app resource named `serving-endpoint`, read into DATABRICKS_SERVING_ENDPOINT_NAME by app.yaml. The endpoint itself is created by databricks.agents.deploy(), not by the bundle.",bundleVariable:"serving_endpoint_name",agentKey:null,appEnvVar:"DATABRICKS_SERVING_ENDPOINT_NAME",actualFromCheck:"agent-endpoint",namesRemoteObject:true,applyWith:`${APP_RELEASE}   # after changing the app resource in databricks.yml`,stageable:false},{id:"llm-endpoint",label:"Foundation model",kind:"model",changedBy:"model-version",arrivesBy:"MLflow model_config, baked by agent/log_model.py at log time, or PLAYER_INSIGHTS_LLM_ENDPOINT when the app release wrote the same bundle variable into the container.",bundleVariable:"llm_endpoint",agentKey:"llm_endpoint",appEnvVar:"PLAYER_INSIGHTS_LLM_ENDPOINT",actualFromCheck:"llm-endpoint",namesRemoteObject:true,applyWith:AGENT_RELEASE,stageable:true},{id:"llm-gateway",label:"AI Gateway",kind:"model",changedBy:"model-version",arrivesBy:"MLflow model_config, baked by agent/log_model.py at log time.",bundleVariable:"llm_gateway",agentKey:"llm_gateway",appEnvVar:null,actualFromCheck:null,namesRemoteObject:true,applyWith:AGENT_RELEASE,stageable:true},{id:"genie-data",label:"Data Genie space",kind:"genie-space",changedBy:"model-version",arrivesBy:"MLflow model_config, and a DatabricksGenieSpace resource on the same model version.",bundleVariable:"genie_data_space_id, naming a space the bundle attaches to rather than creates",agentKey:"data_genie_space_id",appEnvVar:"PLAYER_INSIGHTS_DATA_GENIE_ID",actualFromCheck:"genie-data",namesRemoteObject:true,applyWith:AGENT_RELEASE,stageable:true},{id:"sql-warehouse",label:"SQL warehouse",kind:"sql-warehouse",changedBy:"model-version",arrivesBy:"MLflow model_config, and a DatabricksSQLWarehouse resource on the same model version.",bundleVariable:"warehouse_id",agentKey:"warehouse_id",appEnvVar:"DATABRICKS_SQL_WAREHOUSE_ID",actualFromCheck:"sql-warehouse",namesRemoteObject:true,applyWith:AGENT_RELEASE,stageable:true},{id:"catalog",label:"App catalog",kind:"unity-catalog",changedBy:"model-version",arrivesBy:"MLflow model_config, baked by agent/log_model.py at log time.",bundleVariable:"app_catalog",agentKey:"catalog",appEnvVar:"PLAYER_INSIGHTS_CATALOG",actualFromCheck:null,namesRemoteObject:true,applyWith:AGENT_RELEASE,stageable:true},{id:"schema",label:"App schema",kind:"unity-catalog",changedBy:"model-version",arrivesBy:"MLflow model_config, baked by agent/log_model.py at log time.",bundleVariable:"app_schema",agentKey:"schema",appEnvVar:"PLAYER_INSIGHTS_SCHEMA",actualFromCheck:null,namesRemoteObject:true,applyWith:AGENT_RELEASE,stageable:true},{id:"catalog-allowlist",label:"Data catalogs",kind:"unity-catalog",changedBy:"model-version",arrivesBy:"MLflow model_config; the table list it produces is baked alongside it.",bundleVariable:"data_catalogs",agentKey:"catalog_allowlist",appEnvVar:null,actualFromCheck:null,namesRemoteObject:false,applyWith:AGENT_RELEASE,stageable:true},{id:"catalog-denylist",label:"Excluded tables",kind:"unity-catalog",changedBy:"model-version",arrivesBy:"MLflow model_config, baked by agent/log_model.py at log time.",bundleVariable:"catalog_denylist",agentKey:"catalog_denylist",appEnvVar:null,actualFromCheck:null,namesRemoteObject:false,applyWith:AGENT_RELEASE,stageable:true},{id:"declared-manifest",label:"Declared tables",kind:"unity-catalog",changedBy:"model-version",arrivesBy:"Generated by agent/preflight.py during the log, then baked into the artifact.",bundleVariable:null,agentKey:"declared_manifest",appEnvVar:null,actualFromCheck:"declared-manifest",namesRemoteObject:true,applyWith:AGENT_RELEASE,stageable:false},{id:"max-output-tokens",label:"Answer length limit",kind:"model",changedBy:"model-version",arrivesBy:"MLflow model_config, baked by agent/log_model.py at log time.",bundleVariable:"max_output_tokens",agentKey:"max_output_tokens",appEnvVar:null,actualFromCheck:null,namesRemoteObject:false,applyWith:AGENT_RELEASE,stageable:true},{id:"lakebase",label:"Lakebase (Postgres)",kind:"lakebase",changedBy:"app-redeploy",arrivesBy:"The app resource named `postgres`, read into LAKEBASE_ENDPOINT by app.yaml.",bundleVariable:"lakebase_project_id / lakebase_branch_id / lakebase_database_id",agentKey:null,appEnvVar:"LAKEBASE_ENDPOINT",actualFromCheck:"lakebase-storage",namesRemoteObject:true,applyWith:`${APP_RELEASE}   # after changing the app resource in databricks.yml`,stageable:false},{id:"lakebase-schema",label:"Lakebase schema",kind:"lakebase",changedBy:"app-redeploy",arrivesBy:"The compatibility schema variable, resolved from var.lakebase_app_schema at release time. A source-only Git deploy maps its historical compatibility value to the app-owned adapt schema. Created by the app on boot; bundle targets keep their configured schema.",bundleVariable:"lakebase_app_schema",agentKey:null,appEnvVar:"PLAYER_INSIGHTS_APP_SCHEMA",actualFromCheck:null,namesRemoteObject:false,applyWith:"Set var.lakebase_app_schema and release the app so PLAYER_INSIGHTS_APP_SCHEMA updates.\nThen grant on the new schema (scripts/grant-app-db-access.mjs) and migrate any data.\nChanging the variable alone does not move existing tables.",stageable:false},{id:"assets-volume",label:"Assets volume",kind:"volume",changedBy:"app-redeploy",arrivesBy:"Created empty by the bundle. Nothing in a deploy writes to it.",bundleVariable:"volume",agentKey:null,appEnvVar:null,actualFromCheck:null,namesRemoteObject:false,applyWith:"Set var.volume and redeploy the bundle.",stageable:false},{id:"experiment-id",label:"MLflow experiment",kind:"observability",changedBy:"app-runtime",arrivesBy:'PLAYER_INSIGHTS_EXPERIMENT_ID, resolved from var.experiment_path at release time, or PLAYER_INSIGHTS_EXPERIMENT_PATH resolved to an id at runtime when the id is empty (a "From Git" deploy, which never runs the release). The app reads a saved override first, so a deployment whose experiment did not exist at release can fix the link without a redeploy.',bundleVariable:"experiment_path",agentKey:null,appEnvVar:"PLAYER_INSIGHTS_EXPERIMENT_ID",actualFromCheck:"experiment-id",namesRemoteObject:false,applyWith:"Save it here, or set var.experiment_path and release the app.",stageable:false},{id:"judge-endpoint",label:"Benchmark judge model",kind:"model",changedBy:"app-runtime",arrivesBy:"PLAYER_INSIGHTS_JUDGE_ENDPOINT, read per benchmark run. The app reads a saved override first, then the variable, then a compiled default.",bundleVariable:"judge_endpoint",agentKey:null,appEnvVar:"PLAYER_INSIGHTS_JUDGE_ENDPOINT",actualFromCheck:null,namesRemoteObject:true,applyWith:"Save it here, or set var.judge_endpoint and release the app.",stageable:false},{id:"notebook-declaration",label:"Notebook declarations table",kind:"unity-catalog",changedBy:"app-runtime",arrivesBy:"The three-part Unity Catalog name of the table a notebook publishes to, not the notebook itself. A notebook run appends one row saying what it was configured with, and the app reads the newest row as the signed-in user on each settings read, so a value saved here is used on the next one. Which notebook published it is in the row, so this page can name the notebook without being told it. PLAYER_INSIGHTS_NOTEBOOK_DECLARATION supplies the initial value.",bundleVariable:null,agentKey:null,appEnvVar:"PLAYER_INSIGHTS_NOTEBOOK_DECLARATION",actualFromCheck:"notebook-declaration",namesRemoteObject:true,applyWith:"Save it here, or set PLAYER_INSIGHTS_NOTEBOOK_DECLARATION before releasing the app.",stageable:false},{id:"shared-conversation-rail",label:"Shared conversation rail",kind:"app-behaviour",changedBy:"app-redeploy",arrivesBy:"PLAYER_INSIGHTS_SHARED_CONVERSATION_RAIL, resolved from the bundle at release time.",bundleVariable:"shared_conversation_rail",agentKey:null,appEnvVar:"PLAYER_INSIGHTS_SHARED_CONVERSATION_RAIL",actualFromCheck:null,namesRemoteObject:false,applyWith:`${APP_RELEASE}   # after setting var.shared_conversation_rail`,stageable:false}];var BY_ID=new Map(CONNECTED_RESOURCES.map(resource=>[resource.id,resource]));function connectedResource(id){return BY_ID.get(id)}var RUNTIME_EDITABLE_IDS=CONNECTED_RESOURCES.filter(resource=>resource.changedBy==="app-runtime").map(resource=>resource.id);var STAGEABLE_IDS=CONNECTED_RESOURCES.filter(resource=>resource.stageable).map(resource=>resource.id);export{MLFLOW_JUDGE_PROMPT_VERSION,DEFAULT_JUDGE_ENDPOINT,judgeBadgeLabel,judgeDisclosure,GROUNDEDNESS_BASIS,judgeProvenance,BUDGET_TRUNCATION_CODE,SUITE_CANCELLED_CODE,BENCHMARK_RUNNER_VERSION,withDeadline,CHANGED_BY,CONNECTED_RESOURCES,connectedResource};
