
import{parseServedModel}from"./chunk-BZ3SX762.mjs";import{CONNECTED_RESOURCES}from"./chunk-LOICDKUA.mjs";var DECLARED_RESOURCE_TYPES=["catalog","schema","table","metric-view","sql-warehouse","serving-endpoint","agent-endpoint","mcp-server","genie-space","volume"];var DECLARABLE_KEYS={catalog_allowlist:{label:"Readable scopes",flow:"refused",reason:"The scopes this names are enumerated when the model is logged, and the table list it produces is what the serving principal is granted. Widening it from a published document would move a boundary Unity Catalog holds."},catalog_denylist:{label:"Excluded tables",flow:"needs-model-version",reason:"Applied when the table list is generated, before the model is logged. Narrowing takes a new model version, the same as widening."},warehouse_id:{label:"SQL warehouse",flow:"needs-model-version",reason:"Named as a resource on the model version, which is what grants the endpoint access to it."},data_genie_space_id:{label:"Data Genie space",flow:"needs-model-version",reason:"Named as a resource on the model version, which is what grants the endpoint access to it."},dictionary_genie_space_id:{label:"Dictionary Genie space",flow:"needs-model-version",reason:"Named as a resource on the model version, which is what grants the endpoint access to it."},llm_endpoint:{label:"Foundation model",flow:"needs-model-version",reason:"Read from the model artifact when the container starts."},max_output_tokens:{label:"Answer length limit",flow:"needs-model-version",reason:"Read from the model artifact when the container starts."},catalog:{label:"Unity Catalog catalog",flow:"needs-model-version",reason:"Read from the model artifact when the container starts."},schema:{label:"Unity Catalog schema",flow:"needs-model-version",reason:"Read from the model artifact when the container starts."},max_turns:{label:"Reasoning turn cap",flow:"needs-model-version",reason:"A loop bound inside the orchestrator. Nothing outside the model artifact is read for it."},knowledge_dir:{label:"Knowledge files",flow:"needs-model-version",reason:"Compiled into the model artifact when it is logged. The running endpoint opens no volume for it."},upload_volume:{label:"Attachment staging volume",flow:"needs-model-version",reason:"Compiled into the model artifact when it is logged."}};function declarationFlow(key){return DECLARABLE_KEYS[key]?.flow??"needs-model-version"}var SCOPES_KEY="catalog_allowlist";var MAX_DECLARATION_BYTES=64*1024;var MAX_DECLARED_CONNECTIONS=200;var DECLARABLE_KINDS=["genie-space","sql-warehouse","unity-catalog","volume","model","agent"];function isDeclarableKind(value){return typeof value==="string"&&DECLARABLE_KINDS.includes(value)}function isDeclaredResourceType(value){return typeof value==="string"&&DECLARED_RESOURCE_TYPES.includes(value)}function field(value,limit=500){return typeof value==="string"?value.trim().slice(0,limit):""}function parseDeclaration(raw){if(typeof raw!=="object"||raw===null||Array.isArray(raw))return null;const document=raw;const settings=[];const seenKeys=new Set;let emptyScopes=false;const rawSettings=document.settings;if(typeof rawSettings==="object"&&rawSettings!==null&&!Array.isArray(rawSettings)){for(const[key,value]of Object.entries(rawSettings)){const name=field(key,120);const stated=field(value);if(name===SCOPES_KEY&&!stated)emptyScopes=true;if(!name||!stated||seenKeys.has(name))continue;seenKeys.add(name);settings.push({key:name,value:stated})}}const connections=[];const seenIds=new Set;const rawConnections=document.connections;if(Array.isArray(rawConnections)){for(const entry of rawConnections.slice(0,MAX_DECLARED_CONNECTIONS)){if(typeof entry!=="object"||entry===null)continue;const record=entry;const id=field(record.id,120);const value=field(record.value);if(!id||!value||seenIds.has(id)||!isDeclarableKind(record.kind))continue;seenIds.add(id);connections.push({id,label:field(record.label,200)||id,kind:record.kind,resourceType:isDeclaredResourceType(record.resourceType??record.resource_type)?record.resourceType??record.resource_type:void 0,value,note:field(record.note)})}}if(settings.length===0&&connections.length===0)return null;return{source:field(document.source),revision:field(document.revision,120),publishedAt:field(document.published_at??document.publishedAt,60),publishedBy:field(document.published_by??document.publishedBy,200),settings,connections,emptyScopes}}function compareDeclaration(declaration,live){return declaration.settings.map(setting=>{const flow=declarationFlow(setting.key);const running=live[setting.key]??"";let verdict;if(!running){verdict="unknown"}else if(running===setting.value){verdict="agrees"}else{verdict=flow==="refused"?"refused":"pending"}return{key:setting.key,label:DECLARABLE_KEYS[setting.key]?.label??setting.key,declared:setting.value,live:running,flow,verdict}})}var APPLY_ENV_VARS={catalog:"PLAYER_INSIGHTS_CATALOG",schema:"PLAYER_INSIGHTS_SCHEMA",warehouse_id:"PLAYER_INSIGHTS_WAREHOUSE_ID",data_genie_space_id:"PLAYER_INSIGHTS_DATA_GENIE_ID",llm_endpoint:"PLAYER_INSIGHTS_LLM_ENDPOINT",llm_gateway:"PLAYER_INSIGHTS_LLM_GATEWAY",catalog_allowlist:"PLAYER_INSIGHTS_CATALOG_ALLOWLIST",catalog_denylist:"PLAYER_INSIGHTS_CATALOG_DENYLIST",max_output_tokens:"PLAYER_INSIGHTS_MAX_OUTPUT_TOKENS"};var APPLYABLE_KEYS=new Set(Object.keys(APPLY_ENV_VARS));var NOTEBOOK_REFUSED_KEYS=new Set(["catalog_allowlist"]);var LABELS=Object.fromEntries(CONNECTED_RESOURCES.filter(resource=>resource.agentKey).map(resource=>[resource.agentKey,resource.label]));function text(value){if(typeof value==="string")return value.trim();if(typeof value==="number"||typeof value==="boolean")return String(value).trim();return""}function intendedFromResources(resources){const out={};for(const entry of resources??[]){const key=entry.resource?.agentKey;if(entry.intended===null||entry.intended===void 0)continue;const intended=text(entry.intended);if(!key||!intended&&key!=="llm_gateway"||!APPLYABLE_KEYS.has(key))continue;out[key]=intended}return out}function settingsFromDeclaration(declaration){const out={};if(!declaration)return out;for(const setting of declaration.settings){const key=text(setting.key);const value=text(setting.value);if(!key||!value)continue;if(NOTEBOOK_REFUSED_KEYS.has(key))continue;if(!APPLYABLE_KEYS.has(key))continue;if(declarationFlow(key)==="refused")continue;out[key]=value}return out}function resolveApplyPlan(input){const intended=input.intended??{};const notebook=input.notebook??{};const knobs=[];const notes=[];for(const key of[...APPLYABLE_KEYS].sort()){const envVar=APPLY_ENV_VARS[key];if(!envVar)continue;if(Object.prototype.hasOwnProperty.call(intended,key)){knobs.push({key,label:LABELS[key]??key,value:intended[key],source:"intended",envVar});continue}if(notebook[key]){knobs.push({key,label:LABELS[key]??key,value:notebook[key],source:"notebook",envVar})}}if(knobs.some(knob=>knob.key==="catalog_allowlist"&&knob.source==="intended")){notes.push("Readable scopes were staged by an administrator. If the new list is wider than the live model, the release needs an explicit widen approval.")}if(knobs.some(knob=>knob.source==="notebook")&&!knobs.some(knob=>knob.source==="intended")){notes.push("Values come from the notebook declaration. Intended settings on Connections override the notebook when both name the same key.")}const gateway=knobs.find(knob=>knob.key==="llm_gateway"&&knob.source==="intended");const gatewayModel=knobs.find(knob=>knob.key==="llm_endpoint");if(gateway){notes.push(`AI Gateway release pair: ${gateway.value||"Direct"} with ${gatewayModel?.value||"(missing model)"}. The notebook helper revalidates it before claiming the release. Rollback is Direct plus the existing endpoint through the same confirmed release.`)}const target=text(input.target)||"<your-target>";return{knobs,notes,hasOverrides:knobs.length>0,command:`TARGET=${target} bundle/apply-declaration.sh --apply --i-am-deploying`}}var BAKED_TTL_MS=45e3;var EXTRA_ENV={declared_manifest:"PLAYER_INSIGHTS_DECLARED_MANIFEST",tables:"PLAYER_INSIGHTS_TABLES",manifest_source:"PLAYER_INSIGHTS_MANIFEST_SOURCE"};var LIST_KEYS=new Set(["catalog_allowlist","catalog_denylist","declared_manifest","tables"]);function text2(value){if(typeof value==="string")return value.trim();if(typeof value==="number"||typeof value==="boolean")return String(value).trim();return""}function implicitServedModel(endpoint){const config=asRecord(asRecord(endpoint).config);const entities=Array.isArray(config.served_entities)?config.served_entities:Array.isArray(config.servedEntities)?config.servedEntities:[];if(entities.length!==1)return{entityName:"",version:""};const entity=asRecord(entities[0]);return{entityName:text2(entity.entity_name??entity.entityName??entity.model_name??entity.modelName),version:text2(entity.entity_version??entity.entityVersion??entity.model_version??entity.modelVersion)}}function asRecord(value){return value&&typeof value==="object"&&!Array.isArray(value)?value:{}}function unquote(value){const trimmed=value.trim();if(trimmed.startsWith('"')&&trimmed.endsWith('"')||trimmed.startsWith("'")&&trimmed.endsWith("'")){return trimmed.slice(1,-1)}return trimmed}function parseModelConfigDocument(source){const trimmed=source.trim();if(!trimmed)return{};if(trimmed.startsWith("{")){try{const parsed=JSON.parse(trimmed);const record=asRecord(parsed);const nested=asRecord(record.model_config);if(Object.keys(nested).length>0)return nested;const flavors=asRecord(asRecord(record.flavors).python_function);const fromFlavor=asRecord(flavors.config??flavors.model_config);if(Object.keys(fromFlavor).length>0)return fromFlavor;if("llm_endpoint"in record||"declared_manifest"in record){return record}}catch{return{}}}const block=yamlBlock(source,["model_config:","config:"]);return block?parseYamlMap(block):{}}function yamlBlock(source,headers){const lines=source.split(/\r?\n/);for(let index=0;index<lines.length;index+=1){const line=lines[index];const header=headers.find(name=>line.trimStart().startsWith(name));if(!header)continue;const indent=line.match(/^\s*/)?.[0].length??0;const rest=line.trimStart().slice(header.length).trim();const collected=[];if(rest&&rest!=="|"&&rest!==">"&&rest!=="{}"){collected.push(`_inline: ${rest}`)}for(let next=index+1;next<lines.length;next+=1){const candidate=lines[next];if(!candidate.trim()){collected.push(candidate);continue}const candidateIndent=candidate.match(/^\s*/)?.[0].length??0;if(candidateIndent<=indent)break;collected.push(candidate.slice(indent+2))}const body=collected.join("\n").trim();if(body)return body}return""}function parseYamlMap(block){const result={};const lines=block.split(/\r?\n/);let currentKey="";let list=null;const flushList=()=>{if(currentKey&&list)result[currentKey]=list;list=null};for(const line of lines){if(!line.trim()||line.trimStart().startsWith("#"))continue;const item=line.match(/^\s*-\s+(.*)$/);if(item&&currentKey){list=list??[];list.push(unquote(item[1]));continue}const pair=line.match(/^\s*([A-Za-z0-9_]+):\s*(.*)$/);if(!pair)continue;flushList();currentKey=pair[1];const raw=pair[2].trim();if(!raw||raw==="|"||raw===">"||raw==="[]"){list=raw==="[]"?[]:[];if(raw==="[]"){result[currentKey]=[];list=null}continue}if(raw.startsWith("[")&&raw.endsWith("]")){try{const parsed=JSON.parse(raw.replace(/'/g,'"'));result[currentKey]=Array.isArray(parsed)?parsed.map(entry=>text2(entry)):unquote(raw)}catch{result[currentKey]=unquote(raw)}continue}if(raw==="null"||raw==="~"){result[currentKey]="";continue}result[currentKey]=unquote(raw)}flushList();return result}function artifactText(body){if(typeof body==="string")return body;const record=asRecord(body);if(typeof record.content==="string")return record.content;if(typeof record.data==="string"){try{return Buffer.from(record.data,"base64").toString("utf8")}catch{return record.data}}if(typeof record.text==="string")return record.text;return""}function runIdOf(body){const record=asRecord(body);const version=asRecord(record.model_version??record.modelVersion);return text2(version.run_id??version.runId??record.run_id??record.runId)}function modelIdOf(body){const record=asRecord(body);const version=asRecord(record.model_version??record.modelVersion);const source=text2(version.source??record.source);return/^models:\/(m-[A-Za-z0-9_-]+)$/.exec(source)?.[1]??""}function envVarFor(key){return APPLY_ENV_VARS[key]??EXTRA_ENV[key]??""}function configurationFromBaked(config){const entries=[];for(const[key,raw]of Object.entries(config)){if(raw===void 0||raw===null||raw==="")continue;const value=LIST_KEYS.has(key)?Array.isArray(raw)?raw.map(item=>text2(item)).filter(Boolean):text2(raw).split(",").map(item=>item.trim()).filter(Boolean):text2(raw);if(Array.isArray(value)?value.length===0:!value)continue;entries.push({key,env_var:envVarFor(key),value,source:"artifact",mutability:"model-version",baked:true,required:false})}return entries}async function defaultGetJson(path,query={}){const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");const client=new WorkspaceClient({});return client.apiClient.request({path,method:"GET",query,headers:new Headers({Accept:"application/json"}),raw:false})}async function defaultDownloadText(path){const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");const response=await new WorkspaceClient({}).files.download({file_path:path});const chunks=[];for await(const chunk of response.contents??[])chunks.push(Buffer.from(chunk));return Buffer.concat(chunks).toString("utf8")}async function describeEndpoint(name){const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");return new WorkspaceClient({}).servingEndpoints.get({name})}async function readModelConfigText(transport,runId){const paths=["agent/MLmodel","MLmodel","agent/model_config","model_config.json","agent/config.json"];for(const artifact of paths){try{if(transport.getText){const textBody=await transport.getText("/api/2.0/mlflow/artifacts/get",{run_id:runId,path:artifact});if(textBody.trim())return textBody}const body=await transport.getJson("/api/2.0/mlflow/artifacts/get",{run_id:runId,path:artifact});const extracted=artifactText(body);if(extracted.trim())return extracted}catch{}}try{const listed=asRecord(await transport.getJson("/api/2.0/mlflow/artifacts/list",{run_id:runId,path:"agent"}));const files=Array.isArray(listed.files)?listed.files:[];const mlmodel=files.find(file=>{const path2=text2(asRecord(file).path);return path2.endsWith("MLmodel")||path2.endsWith("model_config")||path2.endsWith("config.json")});const path=text2(asRecord(mlmodel).path);if(!path)return"";const body=await transport.getJson("/api/2.0/mlflow/artifacts/get",{run_id:runId,path});return artifactText(body)}catch{return""}}async function artifactsForServedModel(transport,entityName,version){const attempts=[{path:`/api/2.1/unity-catalog/models/${encodeURIComponent(entityName)}/versions/${encodeURIComponent(version)}`,query:{}},{path:"/api/2.1/unity-catalog/model-versions/get",query:{full_name:entityName,version}},{path:"/api/2.0/mlflow/databricks/model-versions/get",query:{name:entityName,version}},{path:"/api/2.0/mlflow/model-versions/get",query:{name:entityName,version}}];for(const attempt of attempts){try{const body=await transport.getJson(attempt.path,attempt.query);const reference={runId:runIdOf(body),modelId:modelIdOf(body)};if(reference.runId||reference.modelId)return reference}catch{}}return{runId:"",modelId:""}}async function readLoggedModelConfigText(transport,modelId){if(!modelId||!transport.downloadText)return"";const body=asRecord(await transport.getJson(`/api/2.0/mlflow/logged-models/${encodeURIComponent(modelId)}`));const model=asRecord(body.model);const info=asRecord(model.info??body.info);const artifactUri=text2(info.artifact_uri??info.artifactUri);const match=/databricks\/mlflow-tracking\/([^/]+)\/logged_models\/([^/]+)(\/.*)?$/.exec(artifactUri);if(!match||match[2]!==modelId)return"";const relative=(match[3]??"").replace(/\/$/,"");const root=`/WorkspaceInternal/Mlflow/Artifacts/${match[1]}/LoggedModels/${modelId}${relative}`;return transport.downloadText(`${root}/MLmodel`)}var cache=null;function forgetBakedModelConfig(){cache=null}async function readBakedModelConfig(input={}){const endpointName=(input.endpointName??process.env.DATABRICKS_SERVING_ENDPOINT_NAME??"").trim();if(!endpointName)return[];if(!input.readEndpoint&&!input.transport&&!(process.env.DATABRICKS_HOST??"").trim())return[];const now=input.now??Date.now();const transport=input.transport??{getJson:defaultGetJson,downloadText:defaultDownloadText};try{const endpoint=await(input.readEndpoint??describeEndpoint)(endpointName);const parsed=parseServedModel(endpointName,endpoint);const served=parsed.entityName&&parsed.version?{entityName:parsed.entityName,version:parsed.version}:implicitServedModel(endpoint);if(!served.entityName||!served.version)return[];if(cache&&cache.endpoint===endpointName&&cache.entityName===served.entityName&&cache.version===served.version&&now-cache.at<BAKED_TTL_MS){return cache.entries}const artifacts=await artifactsForServedModel(transport,served.entityName,served.version);const document=(artifacts.modelId?await readLoggedModelConfigText(transport,artifacts.modelId).catch(()=>""):"")||(artifacts.runId?await readModelConfigText(transport,artifacts.runId):"");if(!document)return[];const entries=configurationFromBaked(parseModelConfigDocument(document));cache={at:now,endpoint:endpointName,entityName:served.entityName,version:served.version,entries};return entries}catch(error){console.warn("[settings] The served model version’s baked configuration could not be read:",error.message);return[]}}export{DECLARED_RESOURCE_TYPES,MAX_DECLARATION_BYTES,DECLARABLE_KINDS,parseDeclaration,compareDeclaration,APPLY_ENV_VARS,intendedFromResources,settingsFromDeclaration,resolveApplyPlan,parseModelConfigDocument,configurationFromBaked,forgetBakedModelConfig,readBakedModelConfig};
